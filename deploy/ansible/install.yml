---
- name: Installing cQube NVSK
  hosts: localhost
  gather_facts: true
  remote_user: root
  vars_files:
    - ../config.yml

  roles:
          - { role: java, tags: install }
          - createdb
          - angular
          - cqube_conf
