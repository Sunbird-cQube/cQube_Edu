export const environment = {
  production: true,
  apiEndpoint: "{{ protocol }}://{{ api_endpoint }}/admin_dashboard",
  appUrl: "{{ protocol }}://{{ api_endpoint }}",
  dashboardUrl: "{{ protocol }}://{{ api_endpoint }}",
  grafanaEndPoint: "{{ protocol }}://{{ api_endpoint }}/grafana",
  keycloakUrl: "{{ protocol }}://{{ api_endpoint }}/auth",
  realm: "{{ realm_name }}",
  clientId: "{{ admin_app_client_id }}",
  storageType: "{{ storage_type }}",
  report_viewer_config_otp: {% if report_viewer_config_otp == True %}true{% endif %}{% if report_viewer_config_otp == False %}false{% endif %},
  auth_api: "{{ auth_api }}"
};
